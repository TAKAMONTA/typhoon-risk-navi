# MEMORY

## 2026-07-29 地図表示・現在地の不具合修正

### 症状
- トップ（地図タブ）に地図情報が出ない
- 現在地がずれている

### 根本原因
1. **カメラが台風中心に無条件追従**（TyphoonViewModel.mapRegion）。台風13号ドルフィンは沖縄の東約4,400km（マーシャル諸島付近）にあり、地図が太平洋のど真ん中を映していた。沖縄も拠点も現在地も画面外。
2. **サマリーカードが画面全体を占有**し、地図が視覚的に完全に隠れていた。
3. **ライブ現在地表示が存在しなかった**。「現在地」に見えていたのは拠点保存時のスナップショット静的ピン。精度も kCLLocationAccuracyKilometer で約1kmずれ得た。
4. 拠点ピンが risks 配列を基準に描画されており、台風がない日はピンが1つも出なかった。

データ層（JMA fetch/parse）は正常だった。curl で targetTc.json / specifications.json を叩き、JMAParser の期待形式と一致することを確認済み。

### 決定事項
- **遠方しきい値は1200km**。これを超えたら地図は沖縄側を維持し、台風へは「台風」ボタンで移動する。却下案: 常に両方を収める領域にする（沖縄が豆粒になり用をなさない）／常に台風中心（元の挙動、本件の原因）。
- 1200km以内なら拠点と台風の両方が入る領域を出す。この範囲では経度180度をまたぐことが幾何的にありえないため、日付変更線の特別扱いは不要と判断。
- **サマリーカードは既定で折りたたむ**。対策・警報・自治体は「対策・警報・自治体を見る」で展開（最大高300ptのScrollView）。地図が主役という前提を崩さないため。
- **位置情報の許可要求は「現在地」ボタン等のユーザー操作時のみ**。起動時に自動で出すとオンボーディングの「現在地を追加を押した時だけ使います」という説明と矛盾し、審査リスクにもなる。拒否済みなら設定アプリへ誘導するアラートを出す（無反応にしない）。
- 距離の基準名は「現在地」ではなく実際の代表地点名（例:「恩納村の東 約4448km」）。保存場所がなければ「沖縄本島」。

### 落とし穴（次回のため）
- カード上のタップが背後の地図に貫通し、Apple の法的情報ページが開いていた。**タップ吸収は背面（.background 内の Shape に .onTapGesture）に置くこと**。カード本体に .onTapGesture を付けると内部の Link / Button を飲み込む。
- 回帰防止テスト: ios/TyphoonRiskNaviTests/MapRegionTests.swift（遠方台風でカメラが沖縄から動かないことを固定）。

### 検証
- 46テスト全通過。シミュレータ（iPhone 17 Pro）で地図表示・拠点ピン・現在地の青点（那覇に正しく表示）・台風／沖縄／現在地ボタン・カード展開・警報リンクを実機確認。

## 2026-07-31 v0.9.4 (build 6) リリース作業

### やったこと
- fix/map-display-and-current-location を main に ff マージ。
- 0.9.4 (build 6) をアーカイブ→App Store Connect にアップロード。処理 VALID、暗号化申告 exempt 設定済み。

### 決定事項・教訓
- **バージョンは 0.9.4**。0.9.3 は 7/22 の build 5 が審査承認済み（READY_FOR_DISTRIBUTION）でトレイン閉鎖のため、同バージョンへの追加アップロードは ITMS-90062/90186 で拒否される。
- **Info.plist の版数直書きが罠だった**。CFBundleVersion が「5」固定で project.yml の CURRENT_PROJECT_VERSION 変更が無視されていた。両キーを `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)` 参照に変更済み。今後は project.yml の1箇所だけ上げればよい。
- **ITSAppUsesNonExemptEncryption=false を Info.plist に追加**（通信は気象庁への HTTPS のみ）。以後のビルドはコンプライアンス質問が自動回答になり、手動申告不要。
- リリース手順: `asc xcode archive` → `asc xcode export` → `asc builds upload --app 6777631827 --wait`（成果物は .asc/artifacts/、git 追跡外）。

### 未了
- ローカル main が origin/main より先行（push 未実施、指示待ち）。
- App Store 版 0.9.4 のバージョン作成・リリースノート・審査提出は未実施。TestFlight のグループ配布も未設定。

## 2026-08-02 デモデータ廃止・台風誤表示修正（0.9.4 build 9）

### 決定事項
- **デモシード（初回投入の架空5地点）を廃止**。ユーザーデータと同じストアに架空データを混ぜていたことが、「恩納村が出続ける」「最寄り自治体が違う」「登録していない土地の通知」の共通根本原因だった。対症療法をやめ根を抜いた（ユーザー指示）。
- 既存端末は起動時に一度だけパージ。**id・name・lat/lon が元定義と完全一致するものだけ削除**（編集済みはユーザーの場所として保持）。フラグ `has_purged_demo_locations_v1`。スクショモード（-screenshotMode YES）だけは表示用にサンプル使用、ストアには書かない。
- **台風の採用条件は「typhoonNumber が全桁数字」**。気象庁は台風未満の熱帯低気圧に英字仮番号（"b"等）を振る。categoryは補助。複数台風は沖縄に近い順。パーサ防御で非数値は「熱帯低気圧」表示。
- representativeLocation はユーザー作成地点を優先（デモしか無いときのみ従来動作）。

### プロセスの教訓（ユーザーから「何重にもチェックして」）
- **検証は「再起動後・既存データあり」を標準条件にする**。まっさら→登録→直後確認は最短で緑になるだけで実利用を代表しない。deviceLocation 揮発バグはこれで見逃した。
- **テストは仕様から書く**。「代表地点はデモシードのまま」と実装の現状をなぞって固定したテストが、恩納村バグを"仕様"としてロックしていた。
- **1つ直したら同じ根本原因の兄弟バグを横展開で探す**（最寄り自治体→危険度→距離基準→通知はすべて同根だった）。
- **ロジックの正しさとデータの網羅性は別**（最寄り判定は正しいがカタログが14/41件しかなかった）。
- 実機同等の移行検証は、旧状態を plist 注入で再現→新ビルド起動→結果確認が有効。simctl の defaults read はドメインを見られないことがある（terminate 後にファイル直読み）。

### 環境の問題（解決済み 2026-08-02）
`~/.claude/settings.json` の env のモデルIDが無効（`anthropic/claude-sonnet-4.6`）でサブエージェントが全滅していた問題は、`claude-opus-5` / `claude-sonnet-5` / `claude-haiku-4-5-20251001` への修正で解決済み。オーケストラ運用（implementer / reviewer / scout 等への委譲）は復旧している。

## 2026-08-02 0.9.4 (build 10) 提出・公開（2026-08-07 確認）

- 8/2 に警報XMLフォールバック（JSON鮮度切れ時に extra.xml → VPWW53 電文を解析）と自治体防災ページ直リンク（19自治体）を実装し build 10 としてアップロード（13:08 JST、VALID）。13:21 JST に App Store 版 0.9.4 を作成し審査提出。
- **リリース方式は AFTER_APPROVAL（審査通過後に自動公開）に設定**。これが「リリース方式を自動に改善」の実体。スクリプトやスケジュールタスクではない。
- 2026-08-07 時点で 0.9.4 は **READY_FOR_SALE（審査通過・公開済み）**。git は main == origin/main でクリーン（7/31 の push 未実施も解消済み）。
- 確認コマンド: `asc versions list --app 6777631827` / `asc builds list --app 6777631827 --limit 3`。
- 残ゴミ: `ios/TyphoonRiskNavi.xcodeproj.bak.*`（xcodegen のバックアップ6個、git 追跡外）。`.asc/artifacts/` の *.log は 7/22 のまま更新されないが実害なし。

## 2026-08-08 「現在地」の名前に登録時刻の説明を付ける（未コミット）

### 発端
ユーザーから「現在地の隣にずっと 11:49 と表示されているのは何？」。調査の結果、`LocationsView` の「現在地を追加」が `現在地 (11:49)` という**時刻入りの文字列を場所名そのものとして生成・永続化**していた。時計ではなく登録時刻の化石。名前は再生成されないため永久に固定される。

### 決定事項
- **時刻は消さず、「何時何分に登録した場所か」が読んで分かる表記にする**（`現在地（11時49分に登録）`）。当初ユーザー指示は「消したい」だったが、消すと複数登録時に全部が同名「現在地」になり、**暴風域の通知本文でどの場所か判別できなくなる**（通知は `risk.locationName` を使う）。この副作用を報告したところ、説明付きで時刻を残す方針に変更された。時刻は皮肉にも唯一の識別子として機能していた。
- **移行はフラグを持たず毎起動実行**。却下案: `has_stripped_current_location_time_v1` のような一度きりフラグ。理由は下記の教訓を参照。
- **時刻書式は自前固定**（`DateFormatter` に `locale: ja_JP` / `dateFormat: "H時mm分"` / `timeZone: .current`）。`Date().formatted(...)` は端末の12/24時間設定とロケールで出力が変わるため使わない。`Locale(identifier:)` はユーザーの時刻表示設定を継承しないことを実測確認済み（`@hours=h12` を明示しても `H時mm分` は書き換わらない）。
- **旧名の時刻は捨てず引き継ぐ**。キャプチャした文字列が `^\d{1,2}[:.]\d{2}$`（純粋な時刻）なら `H時mm分` に正規化、そうでない表記（`午前11:49` / `11:49 AM` / `3:05 pm`）は**解析せずそのまま差し込む**。無理に解析するより情報を失わない方を選んだ。
- **英語ロケールでも名前は日本語のまま**にする割り切り。`en.lproj` は161キー維持されているが、「現在地」自体が元からハードコードで悪化幅が小さいため。気になるなら `L10n` に1本足せば済む。

### 落とし穴（次回のため）
- **一度きりの移行フラグは「取りこぼしを恒久化する」凶器になる**。当初実装は正規表現に一致しなくてもフラグだけ立てて完了扱いにしていた。端末言語が English (UK/豪) だと旧名は `現在地 (3:05 pm)` の形になり非マッチ → その端末だけ永久に直らない（将来正規表現を直してもフラグキーを `_v2` に上げない限り救済不能）。**破壊的操作（デモシード削除）は一度きりが正しいが、名前の正規化のような非破壊・冪等な処理はフラグを持たせない**。この非対称性をコードのコメントに明記した。
- **毎起動実行にする場合、出力が入力パターンに再マッチしないことが唯一の防波堤**。破れると起動のたびに `に登録` が積み重なり名前が回復不能に壊れる。今回は**新表記に全角カッコ `（）`、旧パターンは半角 `\(` を要求**という不変条件で担保。NSRegularExpression は既定で NFKC 正規化しないため U+FF08 が `\(` にマッチする経路はない（実測確認済み）。
- **`Date().formatted(date:.omitted, time:.shortened)` の出力は想像以上に多様**。実測: `11:49` / `午前11:49` / `11:49<U+202F>AM`（狭幅ノーブレークスペース）/ `3:05 pm` / `11.49`（fi/da/id）/ `오전 11:49`。ASCII スペースの `11:49 AM` は**実機が生成しない**ため、それを固定したテストは実在しない入力を守ることになる。
- **テストがアンカーを検証できているか確かめること**。否定ケースが `現在地 (自宅)` のように数字を含まないものばかりだと、正規表現から `^` `$` を外しても全部パスしてしまう。`現在地 (11:49) 追記` のような近傍の外れ値を入れて初めて意味を持つ。

### 検証
- 全92テスト通過（`CurrentLocationNameMigrationTests` 6件 + 既存86件）。
- メインセッションでも独立に Swift で移行関数を再実装し27項目を実測（冪等性・過剰マッチ・境界値）。reviewer も15形×5回連続適用と、登録側/移行側の書式が24時×6分=144組合せで一致することを確認。
- reviewer 判定 APPROVE。
- **シミュレータで移行検証（過去の教訓どおり「既存データあり・再起動」条件）**。旧形式4件を plist 注入（`現在地 (11:49)` / `現在地 (3:05 pm)` / `現在地 (自宅)` / `恩納村`）→ 新ビルドで起動。結果: 前2件が `現在地（11時49分に登録）` `現在地（3:05 pmに登録）` に、後2件は不変。**4回連続起動しても一切変化なし**（毎起動実行の冪等性が実データで確認できた）。座標・通知レベル・id もすべて保持。UI（地図カード・場所一覧の両方）でも表示を確認済み。

### 未了
- **ビルド番号が10のまま**。build 10 は既に READY_FOR_SALE なので、提出するなら project.yml の `CURRENT_PROJECT_VERSION` を11へ上げる必要がある（Info.plist は `$(CURRENT_PROJECT_VERSION)` 参照なので1箇所でよい）。
- 未コミット。コミット・提出はユーザー指示待ち。
- 同じ「分」に2回「現在地を追加」を押すと同名になる（`add` に重複排除なし）。今回は範囲外とした。
