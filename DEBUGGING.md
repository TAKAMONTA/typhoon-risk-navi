# 開発者向けデバッグガイド

## データソースの状況を確認する

### iOSアプリ側
- **地図左下**：現在のデータソース（実データ / デモデータ / 実データ取得失敗）が表示される
- **設定タブ**：詳細なデータソース状況とエラー内容を確認できる
- **場所タブ**：上部に実データ取得失敗時のバナーが表示される

このアプリは iPhone / iPad 単体で動作します。専用バックエンドの起動は不要です。

## よくある状況と対応

### 実データが取れない
- 主データ源は気象庁 (JMA) の台風防災情報 JSON です
- 気象庁の取得に失敗した場合のみ、米軍 JTWC の警告テキスト取得にフォールバックします
- ネットワークが不安定な場合や、台風シーズン外は実データが取得できないことがあります
- その場合は自動でデモデータにフォールバックします
- アプリ上では「実データ取得失敗」または「現在進行中の台風はありません」として表示されます

### 再試行したいとき
- 地図・場所タブのエラーバナー内にある「再試行」ボタン
- 設定タブの「データを再読み込み」ボタン
- ツールバーの更新ボタン

### スクリーンショット撮影時
- `-screenshotMode YES` で起動すると、実データ取得をスキップしてデモデータを表示します
- `-selectedTab N` で起動時のタブを指定できます（0: 地図、1: 場所、2: 設定）

### デバッグログ
- 実データ取得の成否は Xcode のコンソールで確認できます
- リリースビルドでは不要なデバッグ出力は抑制されます

## 保存場所データ

保存場所は端末ローカルの UserDefaults に JSON として保存されます。

- 保存キー: `saved_locations_v1`
- 初回デモ場所の投入フラグ: `has_seeded_demo_locations_v1`
- アプリ削除で保存場所も削除されます

開発中に保存場所をリセットしたい場合は、シミュレータのアプリを削除して再インストールしてください。

## 実データ vs デモデータの違い

- **実データ**：気象庁 (JMA) または JTWC から取得した実際の台風情報 + 風速半径
- **デモデータ**：動作確認用の固定データ

アプリは実データを優先しますが、取得に失敗した場合は自動でデモに切り替わります。
